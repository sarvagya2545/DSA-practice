#include <stdio.h>

void rearrangement(int* arr, int n) {
    // part 1 rearrange such that arr[i] = i
    
}

int main() {
    // rearrange such that arr[i] = i
    int n;

    scanf("%d", &n);

    int arr[n];

    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    rearrangement(arr, n);

}